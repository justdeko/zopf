That draft doesn't run yet. zopf read it back and found:

{{problems}}

This is the draft it read:

```yaml
{{draft}}
```

Fix exactly those problems, changing nothing else, and reply with the whole corrected workflow as
one fenced yaml block. The format is in {{guide}}/SKILL.md and {{guide}}/references/schema.md if you
need it again. Write nothing to disk and run nothing.
